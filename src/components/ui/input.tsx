"use client"; import * as React from "react"; import { cn } from "@/lib/utils"; export function Input(props: React.HTMLAttributes<HTMLInputElement>) { const { type = "text", placeholder, value, onChange, className, ...inputProps } = props; const radiusClass = "rounded-md"; const classes = cn("border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 text-slate-600 dark:text-slate-300 focus-visible:ring-indigo-500", radiusClass, className); return ( <input type={type} placeholder={placeholder} value={value} onChange={onChange} className={cn(classes, radiusClass)} {...inputProps} /> ); }