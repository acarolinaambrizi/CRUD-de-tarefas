"use client"; import * as React from "react"; import { Dialog, DialogOverlay, DialogContent, DialogHeader, DialogTitle, DialogCloseButton } from "@radix-ui/react-dialog"; export const Dialog = ({ isOpen, onClose, children }: { isOpen: boolean; onClose: () => void; children: React.ReactNode; }) => { return ( <DialogOverlay open={isOpen} onClose={onClose}> <Dialog> <DialogOverlay> <DialogContent> {children} </DialogContent> </Dialog> </DialogOverlay> </Dialog> ); };